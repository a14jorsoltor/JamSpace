<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.login">
    android.permission.WRITE_EXTERNAL_STORAGE
    <application

        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Login">

        <activity
            android:name="com.example.login.PantallaRegistre"
            android:parentActivityName="com.example.login.MainActivity" >

            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.login.MainActivity" />
        </activity>
        <activity
            android:name="com.example.login.PantallaPrincipal"
            android:parentActivityName="com.example.login.MainActivity" >

            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.login.MainActivity" />
        </activity>
        <activity
            android:name="com.example.login.Edit_portfoli"
            android:parentActivityName="com.example.login.ViewPortfoli" >

            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.login.ViewPortfoli" />
        </activity>
        <activity
            android:name="com.example.login.ViewPortfoli"
            android:parentActivityName="com.example.login.PantallaPrincipal" >

            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.login.PantallaPrincipal" />
        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>